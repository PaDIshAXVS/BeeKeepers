---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import HoneyCard from '../components/HoneyCard.astro';
import { products } from '../data/products.js';
---

<html lang="ru">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Мёд премиум-класса с пасеки</title>
    <meta name="description" content="Натуральный мёд без сахара и подогрева. Доставка по Москве и области." />
  </head>

  <body>
    <Header />

    <!-- HERO -->
    <section class="hero">
      <div class="hero-text">
        <h1>Мёд премиум-класса</h1>
        <p>Ручной отбор. Без сахара. С нашей пасеки — прямо к вашему столу.</p>
        <a href="/catalog" class="btn">Смотреть каталог</a>
      </div>
      <img src="/images/hero.png" class="hero-image">
    </section>

    <!-- CATALOG -->
    <section id="catalog" class="catalog">
      <div class="container">
        <h2>Каталог мёда</h2>
        <div class="grid">
          {products.map(p => (
            <HoneyCard 
              title={p.title} 
              price={p.price} 
              img={p.img} 
            />
          ))}
        </div>
      </div>
    </section>

    <!-- ABOUT -->
    <section id="about" class="about">
      <div class="container">
        <h2>Почему наш мёд</h2>
        <ul>
          <li>Собираем только в сезон</li>
          <li>Не подогреваем и не добавляем сахар</li>
          <li>Собственная пасека с 2010 года</li>
          <li>Доставка по Москве и области</li>
        </ul>
      </div>
    </section>

    <!-- CONTACTS -->
    <section id="contacts" class="contacts">
      <div class="container">
        <h2>Контакты</h2>
        <p>Телефон / WhatsApp: <a href="https://wa.me/375333800329">+375 333 800329</a></p>
        <p>Адрес: Московская обл., г. Пушкино, ул. Пасечная, д. 1</p>
        <p>Время работы: пн-вс 9:00–19:00</p>
      </div>
    </section>

    <Footer />

    <!-- GLOBAL STYLES -->
    <style>
      :global(:root) {
        --gold: #d4af37;
        --black: #0f0f0f;
        --surface: #1a1a1a;
        --gray: #e5e5e5;
      }
      body {
        margin: 0;
        font-family: system-ui, sans-serif;
        background: var(--black);
        color: var(--gray);
      }
      h1, h2, h3 { color: var(--gold); }
      a { color: var(--gold); }
      .container { max-width: 960px; margin: 0 auto; padding: 0 1.5rem; }
      .btn {
        display: inline-block;
        background: var(--gold);
        color: #000;
        padding: .75rem 2rem;
        border-radius: 4px;
        text-decoration: none;
        font-weight: 600;
      }
      .btn:hover { filter: brightness(1.15); }

      /* HERO */
      .hero {
        position: relative;
        min-height: 70vh;
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
        color: #fff;
      }
      .hero::before {
        content: "";
        position: absolute;
        inset: 0;
        background: rgba(0, 0, 0, .6);
      }
      .hero-text { position: relative; z-index: 1; }
      .hero h1 { font-size: 2.8rem; margin-bottom: 1rem; text-shadow: 0 0 10px rgba(212, 175, 55, .5); }
      .hero p { font-size: 1.2rem; margin-bottom: 2rem; }
      .hero-image { 
        position: absolute; 
        top: 100; 
        left: 0; 
        width: 100%; 
        height: 100%; 
        object-fit: cover; 
        opacity: 0.1;
      }

      /* CATALOG */
      .catalog { padding: 3.5rem 0; }
      .catalog h2 { text-align: center; margin-bottom: 2rem; font-size: 2rem; }
      .grid { display: grid; gap: 1.5rem; grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); }

      /* ABOUT */
      .about { background: var(--surface); padding: 3rem 0; }
      .about ul { list-style: none; padding: 0; max-width: 600px; margin: 0 auto; }
      .about li { padding: .4rem 0; }

      /* CONTACTS */
      .contacts { padding: 3rem 0; }
    </style>
  </body>
</html>